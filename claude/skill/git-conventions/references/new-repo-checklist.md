# New Repo Checklist

1. Create all 11 conventional commit labels (check which already exist first) - see `references/labels.md`
2. Add `.gitignore` before the initial commit
3. First commit message: `Initial commit`
4. Confirm no direct commits to `main`
5. Copy `templates/release.yml` to `.github/release.yml`
